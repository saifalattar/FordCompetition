import 'package:flutter/material.dart';

class CookingBook extends StatelessWidget {
  const CookingBook({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
