import 'package:flutter/material.dart';

class NursingBook extends StatelessWidget {
  const NursingBook({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
